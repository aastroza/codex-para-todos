{
  "name": "codex-para-todos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "deploy": "npm run build && wrangler pages deploy dist --project-name codex-para-todos"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.15.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.45.0"
  }
}
